<strong>Movie List</strong>

<ol class="alternating-colors">
  <% @lists.each do |l| %>
    <li>
      <strong><%= l.name %></strong>
      <p><%= link_to'More...', list_path(l) %></p>
    </li>
  <% end %>
</ol>
<%= link_to 'Create a list', new_list_path, class: 'btn btn-primary' %>
